from manim import *\n\nclass DecisionTree(Scene):\n    def construct(self):\n        # Título de la Animación\n        titulo = Text('Árbol de Decisión', font_size=48).to_edge(UP)\n        self.play(Write(titulo), run_time=2)\n        self.wait(1)\n        \n        # Mover el título a la esquina superior derecha y reducir su tamaño\n        titulo_small = titulo.copy().scale(0.5).to_edge(UP + RIGHT)\n        self.play(Transform(titulo, titulo_small))\n        self.wait(1)\n        \n        # Crear el nodo raíz\n        nodo_raiz = Circle(radius=0.5, color=WHITE, fill_opacity=1)\n        text_raiz = Text('¿Quiero algo dulce?').move_to(nodo_raiz.get_center())\n        self.play(Create(nodo_raiz), Write(text_raiz), run_time=2)\n        self.wait(1)\n        \n        # Crear el nodo de respuesta SI\n        nodo_si = Circle(radius=0.5, color=WHITE, fill_opacity=1).next_to(nodo_raiz, DOWN + LEFT)\n        text_si = Text('¿Quiero un helado?').move_to(nodo_si.get_center())\n        self.play(Create(nodo_si), Write(text_si), run_time=2)\n        self.wait(1)\n        \n        # Crear el nodo de respuesta NO\n        nodo_no = Circle(radius=0.5, color=WHITE, fill_opacity=1).next_to(nodo_raiz, DOWN + RIGHT)\n        text_no = Text('¿Quiero una hamburguesa?').move_to(nodo_no.get_center())\n        self.play(Create(nodo_no), Write(text_no), run_time=2)\n        self.wait(1)\n        \n        # Ligar nodo raíz con nodo de respuesta SI\n        linea_si = Line(start=nodo_raiz.get_bottom(), end=nodo_si.get_top() )\n        self.play(Create(linea_si), run_time=1)\n        \n        # Ligar nodo raíz con nodo de respuesta NO\n        linea_no = Line(start=nodo_raiz.get_bottom(), end=nodo_no.get_top())\n        self.play(Create(linea_no), run_time=1)\n        self.wait(1)\n        \n        # Crear nodos finales para respuesta SI\n        nodo_si_final1 = Circle(radius=0.5, color=WHITE, fill_opacity=1).next_to(nodo_si, DOWN + LEFT)\n        text_si_final1 = Text('Empieza a comer helado').scale(0.8).move_to(nodo_si_final1.get_center())\n        self.play(Create(nodo_si_final1), Write(text_si_final1), run_time=2)\n        self.wait(1)\n        \n        nodo_si_final2 = Circle(radius=0.5, color=WHITE, fill_opacity=1).next_to(nodo_si, DOWN + RIGHT)\n        text_si_final2 = Text('Elegir otro dulce').scale(0.8).move_to(nodo_si_final2.get_center())\n        self.play(Create(nodo_si_final2), Write(text_si_final2), run_time=2)\n        self.wait(1)\n        \n        # Ligar nodos de respuesta SI con nodos finales \n        linea_si_final1 = Line(start=nodo_si.get_bottom(), end=nodo_si_final1.get_top())\n        self.play(Create(linea_si_final1), run_time=1)\n\n        linea_si_final2 = Line(start=nodo_si.get_bottom(), end=nodo_si_final2.get_top())\n        self.play(Create(linea_si_final2), run_time=1)\n        self.wait(1)\n\n        # Crear nodo final para respuesta NO\n        nodo_no_final = Circle(radius=0.5, color=WHITE, fill_opacity=1).next_to(nodo_no, DOWN)\n        text_no_final = Text('Empieza a comer la hamburguesa').scale(0.8).move_to(nodo_no_final.get_center())\n        self.play(Create(nodo_no_final), Write(text_no_final), run_time=2)\n        self.wait(1)\n\n        # Ligar nodo de respuesta NO con nodo final \n        linea_no_final = Line(start=nodo_no.get_bottom(), end=nodo_no_final.get_top())\n        self.play(Create(linea_no_final), run_time=1)\n        self.wait(1)\n\n        # Conclusión de la Animación\n        conclusion = Tex(r\"Un <b>Árbol de Decisión</b> nos ayuda a tomar decisiones basadas en nuestras respuestas a preguntas simples de <b>Sí</b> o <b>No</b>\", font_size=32).shift(DOWN * 3)\n        self.play(Write(conclusion), run_time=3)\n        self.wait(2)\n",
   